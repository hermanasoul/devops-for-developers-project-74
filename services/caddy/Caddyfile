localhost {
    log {
        format json
    }

    # Компрессия
    encode zstd

    # Самоподписной сертификат (для разработки)
    tls internal

    # Проксирование всех запросов в сервис app:8080
    reverse_proxy /* app:8080
}
